# Copy this file to ".env" and fill in your key, then run: npm run server
# The key is read by the proxy (server/index.js) only — it is never sent to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: port for the AI Assist proxy (defaults to 8787).
# AI_PROXY_PORT=8787
